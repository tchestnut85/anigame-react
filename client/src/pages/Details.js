import { Detail } from '../components/Detail/Detail';
import React from 'react';

// import { Loader } from '../components/Loader/Loader';

export const Details = () => {
	return (
		<section className='columns section is-centered is-vcentered'>
			<Detail />
		</section>
	);
};
